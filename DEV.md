Local Development (Vercel-style)

1) Copy the env file:
   - cp .env.example .env
   - Fill in values from server/.env (or your secrets manager).

2) Install dependencies:
   - npm install
   - cd src && npm install

3) Run the app:
   - npm run dev

App URL: http://localhost:5173
API URL: http://localhost:8000/api
